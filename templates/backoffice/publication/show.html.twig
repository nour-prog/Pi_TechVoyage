{% extends 'backoffice/base.html.twig' %}

{% block body %}
    <h1>Publication</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ publication.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ publication.title }}</td>
            </tr>
            <tr>
                <th>ShortDescription</th>
                <td>{{ publication.shortDescription }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ publication.content }}</td>
            </tr>

            <tr>
                <th>Image</th>
                <td>{{ publication.image }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_publication_indexback') }}">back to list</a>

    <a href="{{ path('app_publication_edit', {'id': publication.id}) }}">edit</a>

    {{ include('backoffice/publication/_delete_form.html.twig') }}
{% endblock %}
