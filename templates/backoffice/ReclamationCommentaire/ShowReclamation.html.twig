{% extends 'backoffice/base.html.twig' %}

{% block title %}Commentaire{% endblock %}


{% block body %}
<br>
<br>
<br>
<br>
<br>
<br>
<h1>Liste des commentaires</h1>
<table border="1">
    <tr>
        <td>UserName</td>
        <td>Contenu</td>
        <td>Date de creation</td>
        <td>Réclamation</td>


    </tr>
    {% for i in tabCommentaire %}
        <tr>
            <td>{{ i.getUser().getPrenom() }} {{ i.getUser().getNom() }}</td>
            <td>{{i.contenu}}</td>
            {% if i.dateCreation is not null %}
                <td>{{ i.dateCreation|date }}</td>
            {% else %}
                <td></td>
            {% endif %}
            <td>{{i.Reclamation}}</td>


             <td>
                 <a href = "{{ path('app_deleteCommentaire_back', { id: i.id }) }}"> Supprimer </a>

                 {% if i.getUser().getId() == UserId %}
                 <a href = "{{ path('app_updateCommentaire_back', { id: i.id }) }}"> Modifier </a>
                 {% endif %}

             </td>

        </tr>
    {% endfor %}

            <a href = "{{ path('add_commentaire_back', { id: reclamation.getId() }) }}"> Ajouter </a>

</table>
<br>
<br>
<br>
<br>

<br>
{% endblock %}