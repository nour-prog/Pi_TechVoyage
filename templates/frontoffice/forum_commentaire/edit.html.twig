<!DOCTYPE html>

<title>Edit ForumCommentaire</title>

{% block body %}
    <h1>Edit ForumCommentaire</h1>

    {{ include('forum_commentaire/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_forum_commentaire_index') }}">back to list</a>

    {{ include('forum_commentaire/_delete_form.html.twig') }}
{% endblock %}
